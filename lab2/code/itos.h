/*! @file itos.h
    @author Joshua Green
    @author Andrew Groot
    @brief Two utility functions: int to std::string (decimal and hex).
*/

#include <string>

std::string itos(long int number);
std::string itoshex(long int number);
